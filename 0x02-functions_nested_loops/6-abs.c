#include "main.h"

/**
 * _abs - Print the absolute value of a number
 * @num: the number to be tested
 * Return: The absolute value
 */

int _abs(int num)
{
	if (num < 0)
		num *= -1;
	return (num); 
}
